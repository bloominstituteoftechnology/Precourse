Array: an array is basically a collection of numbers, strings, and other kinds of variable-types arranged according to a sequence of numbers starting with zero. You can add items to or subtract them from an array dynamically, and you can store items of different types within the same array. It can be strings, numbers, and even other arrays within an array. Arrays have a number of their own methods you can use to do different things to and with the array.