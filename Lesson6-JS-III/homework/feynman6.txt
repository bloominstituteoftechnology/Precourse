Arrays: 

An array is a special variable that can store multiple values instead of just one. You can store multiple types of data in an array, and all of the items in the array do not need to be of the same type. Because it can store more than one piece of data, it is a simple data structure. 