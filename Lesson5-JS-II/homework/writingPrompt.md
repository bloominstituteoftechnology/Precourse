* `for` - creates a loop through a statement with three expressions
	* `&&`- checks for two matching variables that are true 'and'
    * `||` - looks for one of two variables that are true 'or'
    * `!` - negates a variable or means 'not'