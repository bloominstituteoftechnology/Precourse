Feynman Writing Prompt

1. Closure -- A closure is a function within a function. It includes the "environment" in which it was created or declared, so it has access to all of the values and parameters in that environment.